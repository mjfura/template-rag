from .entity import ChunkEntity


class ChunkValue(ChunkEntity):
    """
    Value object for a chunk. Represents a concrete instance of a chunk entity.

    Attributes:
        content (str): The content of the chunk.
        metadata (dict): The metadata of the chunk.
    """

    pass
